type Consulta {
    id: ID!
    idMedico: Int!
    idPaciente: Int!
    descricao: String
    diaHoraConsulta: String!
    status: String!
    motivoConsulta: String
}

type Query {
    # Permite filtrar histórico do paciente, opcionalmente apenas futuras
    historicoPaciente(idPaciente: Int!, apenasFuturas: Boolean): [Consulta]
}